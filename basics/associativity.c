/*associativity rule is used because
both the sign are same 
and the rule says solve it 
from left to right*/

#include <stdio.h>

int main(){
    int a = 4*3/6*2;
    int b = 3*5/7*8;
    printf("%d\n",a);
    printf("%d\n",b);
    return 0;
    
}