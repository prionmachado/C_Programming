/*This code shows variable declaration
means you have to first declare the variable then
assigned it
There wont be any output because only variable is declared*/
#include <stdio.h>

int main(){

    int a = 22;
    int b = a;
    int c= b+1;
    int d = 1,e;
    int a,b,c;
    a = b = c = 1;

    return 0;
}